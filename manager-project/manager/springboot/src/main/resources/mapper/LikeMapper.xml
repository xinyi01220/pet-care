<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.LikeMapper">


    <insert id="insert">
        insert into like (article_id, user_id, create_time) values (#{articleId}, #{userId},#{createTime})
    </insert>

    <delete id="deleteById">
        delete from like where like_id = #{likeId}
    </delete>

    <select id="selectUserLike" resultType="com.example.entity.Like">
        select * from like where article_id = #{articleId} and user_id = #{userId}
    </select>

    <select id="selectByArticleId" resultType="java.lang.Integer">
        select count(*) from like where article_id = #{articleId}
    </select>

</mapper>