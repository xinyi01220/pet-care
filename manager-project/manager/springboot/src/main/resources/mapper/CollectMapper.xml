<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CollectMapper">


    <insert id="insert">
        insert into collect (user_id, article_id, create_time) values (#{userId},#{articleId},  #{createTime})
    </insert>

    <delete id="deleteById">
        delete from collect where collect_id = #{collectId}
    </delete>

    <select id="selectUserCollect" resultType="com.example.entity.Collect">
        select * from collect where article_id = #{articleId} and user_id = #{userId}
    </select>

    <select id="selectByArticleId" resultType="java.lang.Integer">
        select count(*) from collect where article_id = #{articleId}
    </select>

</mapper>